channels:
  - conda-forge
  - ome
dependencies:
  - pip
  # ome: minimal dependencies required to use the Python API
  - omero-py
  - pip:
    - omero-rois
    - ome-zarr
    - dask-image
    - stardist
    - tensorflow
